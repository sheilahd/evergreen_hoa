<%= form_for(order) do |o| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field_customer">
  <%= o.label :Neighbor %>
  <div class="customers">
    <%= o.collection_select :customer_id, Customer.all,:id, :fam_name, include_blank: true%>
  </div>
  </div>

  <div class="field_documents">
  <%= o.label :doc_name %>
  <%= o.select(:doc_name, Order.documents) %>
  </div>



  <div class="field">
    <%= o.label :doc_count %>
    <%= o.number_field :doc_count %>
  </div>
<h5>Please add here the document you need if it is not listed above: </h5>
  <div class="field">
    <%#= o.label :description %>
    <%= o.text_area :description %>
  </div>



  <div class="actions">
    <%= o.submit %>
  </div>
<% end %>
