<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field_customer">
    <%= form.label :customer %>
    <div class="customers">
      <%= form.collection_select :customer_id, Customer.all, :id, :fam_name, include_blank: true %>
    </div>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
